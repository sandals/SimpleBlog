<%= simple_form_for [:admin, @article] do |f| %>
	<%= f.input :title %>
	<%= f.input :excerpt %>
	<%= f.input :body, as: :text %>
	<%= f.submit "Save" %>
	<%= f.submit "Publish", name: "publish" %>
<% end %>

